IMAGE UPDATE

The five uploaded jewellery photos and uploaded DAS ENTERPRISES logo have been placed inside the website package.

Customer-side code uses relative paths (./assets/...) so GitHub Pages project deployment does not depend on a domain root.

The localStorage state key was bumped to v3 so an older browser cache does not keep the previous broken image paths.
